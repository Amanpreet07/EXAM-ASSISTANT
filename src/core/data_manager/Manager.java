package core.data_manager;

// Purpose: to deal with directories in the background.
public class Manager {
    
    public static int initFiles(){
        int status = 0; // 0 is error, 1 is all fine.
        
        return status;
    }
    
}
